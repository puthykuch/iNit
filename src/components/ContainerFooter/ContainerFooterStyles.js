import { ColorScheme } from '../../app-config';

const styles = {
  footerContainer: {
    borderTopColor: ColorScheme.tabUnselect,
    backgroundColor: '#FFF'
  },
  title: {
    fontSize: 8,
    color: ColorScheme.tabUnselect
  },
  subTitle: {
    fontSize: 16,
    color: ColorScheme.tabUnselect
  },
  icon: {
    color: ColorScheme.tabUnselect,
    fontSize: 20 
  }
}

export default styles; 